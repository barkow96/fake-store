import { cn } from "@/utils";

export default function Cart() {
  return (
    <div
      className={cn(
        "flex min-h-screen items-center justify-center",
        "bg-background",
        "font-sans",
      )}
    >
      Cart Page
    </div>
  );
}
